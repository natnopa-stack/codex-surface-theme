# 安全说明

## 漏洞报告

一般问题请通过本仓库 Issues 提交。报告中请勿包含本地绝对路径、调试端口、PID、线程 ID、私有截图或其他敏感信息。

## 安全边界（摘要）

- 主题只连接 `127.0.0.1` 上的 Codex 本地调试端点（CDP）；
- 不访问外部网络，不收集、不上传使用数据，也不读取凭据、密钥或 Cookie；
- 不发送模型请求、不创建 Goal，因此不会额外消耗模型 Token；
- 不修改官方安装文件；无注册表项、无后台服务、无独立常驻 watcher；
- 本机运行记录（`runtime.json`、`*.log`）不得提交或分发。

数据边界、本机文件说明与发布包校验命令见 [docs/SECURITY.md](docs/SECURITY.md)。
